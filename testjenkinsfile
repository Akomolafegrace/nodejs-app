pipeline{
    agent any

    stages{
        stage('Dep install'){
            agent { docker { image 'node:18' 
               args '-u root:root'}}
            steps{
                sh 'npm cache clean'
                sh 'npm install'
            }
        }
    }
}